import type { HeroProps } from './Hero.types';

export const HERO_DEFAULT_PROPS: Partial<HeroProps> = {
  title: undefined,
  subtitle: undefined,
  primaryActionLabel: undefined,
  secondaryActionLabel: undefined,
};
